# IEEE 2030.5 Server Configuration

# Base URL of the IEEE 2030.5 server (REQUIRED)
IEEE2030_BASE_URL=https://your-ieee2030-server:port

# Certificate authentication - use either file path or direct value
# Option 1: Single PEM file with both certificate and private key (most common)
IEEE2030_CERT_PATH=/path/to/client.pem

# Option 2: Separate certificate and key files
# IEEE2030_CERT_PATH=/path/to/client.crt
# IEEE2030_KEY_PATH=/path/to/client.key

# Option 3: Certificate content (base64 encoded or PEM format)
# IEEE2030_CERT_VALUE="-----BEGIN CERTIFICATE-----..."
# IEEE2030_KEY_VALUE="-----BEGIN PRIVATE KEY-----..."

# Security settings
IEEE2030_INSECURE=false

# Optional: Request timeout in milliseconds (default: 30000)
IEEE2030_TIMEOUT=30000

# Optional: Custom User-Agent
IEEE2030_USER_AGENT=ieee2030.5-mcp/0.0.0